The main pending task is renaming this, because current behavior exceeds original goal of adding new units to odometers.

Other pending functionalities are the following:

1. Restrict odometer visibility by hiding it for limited-access users and displaying it as read-only for administrators only.

2. Decide when odometers become read-only to prevent unwanted changes.

3. Vehicle Display: Identify screens where showing more than the license plate (like “plate + driver”) makes sense, reflecting the one-car-per-driver setup.
